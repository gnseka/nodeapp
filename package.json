{
  "name": "login",
  "version": "1.0.0",
  "description": "create api for login",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cross-env NODE_OPTIONS='--openssl-legacy-provider' NODE_ENV=development ./node_modules/.bin/netlify-lambda serve src",
    "build": "cross-env NODE_OPTIONS='--openssl-legacy-provider' NODE_ENV=production ./node_modules/.bin/netlify-lambda build src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cross-env": "^7.0.3",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "netlify-lambda": "^2.0.16",
    "serverless-http": "^3.2.0"
  }
}
